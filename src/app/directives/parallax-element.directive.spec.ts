import { ParallaxElementDirective } from './parallax-element.directive';

describe('ParallaxElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxElementDirective();
    expect(directive).toBeTruthy();
  });
});
